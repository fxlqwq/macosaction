name: Run on macOS

on:
  push:
    branches:
      - main  # 可以根据需要修改触发条件
  pull_request:
    branches:
      - main  # 或者在PR上触发

jobs:
  run_on_macos:
    runs-on: macos-latest  # 使用最新的macOS runner
    steps:
      - name: Run sw_vers
        run: sw_vers  # 运行macOS信息命令
